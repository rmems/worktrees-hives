codecov:
  require_ci_to_pass: false

coverage:
  status:
    project:
      default:
        target: 0
        threshold: 0.0
        base: auto
    patch:
      default:
        target: 0
        threshold: 0.0
        base: auto

ignore:
  - "**/target/**"
  - "**/.venv/**"
  - "**/tests/**"
